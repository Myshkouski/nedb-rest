FROM node

ARG WORKDIR=/usr/src/
ARG DB_PORT=27017
ARG DB_PATH=/var/run/

ENV DB_PORT $DB_PORT
ENV DB_PATH $DB_PATH

WORKDIR $WORKDIR

COPY . $WORKDIR

RUN npm install

EXPOSE $DB_PORT

CMD ["npm", "start"]
